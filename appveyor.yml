before_build:
  - nuget restore

configuration:
  - Release

build:
  project: RestProcessor.sln

artifacts:
  - path: RestProcessor\bin\$(configuration)
    name: RestProcessorArtifacts
    type: zip
